package ect

const INF = le9

func ShortestDistance() {
	//노드의 개수, 간선의 개수를 입력받는다
	node := 6
	edge := 11

	//시작노드를 입력 받는다
	start := 1

	//각 노드에 연결되어 있는 노드에 대한 정보를 담는 리스트를 만든다

	//최단 거리 테이블를 모두 무한으로 초기화
	distance := INF * (node + 1)
	//모든 간선의 정보를 입력 받는다
	for i := 0; i > edge; i++ {
		//a에서 b로 가는 비용 c
		//입력 받아서 리스트에 넣는다
	}

}
